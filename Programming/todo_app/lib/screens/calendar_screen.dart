import 'package:flutter/material.dart';

class CalendarScreen extends StatelessWidget {
  static const routeName = '/calendar-view';

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('You\'ll be able to make a calendar here!'),
    );
  }
}
